// Mock dashboard stats untuk development\nexport interface DashboardStats {\n  totalItems: number;\n  totalBookings: number;\n  totalRevenue: number;\n  activeUsers: number;\n}\n\nexport const mockDashboardStats: DashboardStats = {\n  totalItems: 12,\n  totalBookings: 34,\n  totalRevenue: 2450000, // Rp 2,450,000\n  activeUsers: 8,\n};\n\nexport const getDashboardStats = async (): Promise<DashboardStats> => {\n  // In production, this would fetch real data from database\n  // For development with database issues, return mock data\n  if (process.env.NODE_ENV !== 'production') {\n    console.log('[Dashboard] Using mock stats for development');\n    return mockDashboardStats;\n  }\n  \n  // TODO: Implement real database queries when DB is available\n  try {\n    // Real implementation would be:\n    // const items = await prisma.item.count({ where: { isDeleted: false } });\n    // const bookings = await prisma.booking.count();\n    // etc...\n    return mockDashboardStats;\n  } catch (error) {\n    console.log('[Dashboard] Database error, using fallback stats');\n    return mockDashboardStats;\n  }\n};